import React from 'react';
import AppSpinner from '@/helpers/ui/spinners/AppSpinner';

const AppInitialSpinner = () => (
  <div style={{ marginTop: '20%' }}>
    <AppSpinner />
  </div>
);

export default AppInitialSpinner;
